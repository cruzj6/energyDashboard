'use strict';

/**
 * @ngdoc directive
 * @name energydashApp.directive:loginModal
 * @description
 * # loginModal
 */
angular.module('energydashApp')
  .directive('loginModal', function ($rootScope, $location, energyDatabaseService) {
    return {
      link: function postLink(scope, element, attrs) {

      }
    };
  });
